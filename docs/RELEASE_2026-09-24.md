# Studio v3 release

The owner authorized System-first deployment on 2026-09-24. Studio v1 remains the original reference; v2 remains WikiHouse; v3 consumes Cassette 01 from System.

Pinned System release: e7ac75ce48a99b143f68ce940bd849b9e8134b98. Studio must not merge to main until that System release passes its live WikiHouse and cassette browser checks. The Studio Pages workflow checks all three variations before deployment and again against the live site.

Drive v76 remains the master baseline. This GitHub revision is ahead with a new System pin, deployment workflow fixes and live verification. No automatic Drive synchronization. Architecture is unchanged. Engineering and opening limitations remain explicit.

The legacy branch Pages job remains configured. The prepared deployment waits for it to complete so an unprepared repository-root build cannot overwrite the validated runtime.
