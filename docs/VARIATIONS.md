# Studio variations

V1: original reference in dist/v1, byte-identical to the pre-task version. V2: WikiHouse, the default, with existing module/stage controls and opening/end-wall holds. V3: independent OBTP Cassette 01, supplied by System through the same pinned commit relationship. V3 includes nominal end-wall framing but no released openings or weatherproof envelope.

The shell preserves each loaded iframe and its controls when switching. tools/prepare_system.py verifies system.lock.json, copies System dist and substitutes build tokens for all three variation pages. Generated System copies are not source-controlled.

No live deployment is authorized in this batch. Use the downloadable prepared preview from Drive or the non-deploying validation workflow artifact.
